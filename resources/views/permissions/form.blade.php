<x-app-layout>
    <div class="mx-auto max-w-2xl"><div class="mb-8"><a href="{{ route('permissions.index') }}" class="text-sm font-semibold text-blue-600 hover:text-blue-800">← Back to permissions</a><h1 class="mt-3 text-3xl font-bold text-slate-900">{{ $permission->exists ? 'Edit permission' : 'Create permission' }}</h1><p class="mt-2 text-sm text-slate-500">Permissions are assigned to roles to grant access.</p></div><form method="POST" action="{{ $permission->exists ? route('permissions.update', $permission) : route('permissions.store') }}" class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">@csrf @if($permission->exists) @method('PUT') @endif <div class="space-y-6"><div><label class="text-sm font-semibold text-slate-700">Permission name</label><input name="display_name" value="{{ old('display_name', $permission->display_name) }}" required class="mt-2 block w-full rounded-xl border-slate-300 shadow-sm focus:border-blue-500 focus:ring-blue-500">@error('display_name')<p class="mt-1 text-sm text-rose-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-semibold text-slate-700">Group</label><input name="group" value="{{ old('group', $permission->group) }}" placeholder="For example: Administration" class="mt-2 block w-full rounded-xl border-slate-300 shadow-sm focus:border-blue-500 focus:ring-blue-500">@error('group')<p class="mt-1 text-sm text-rose-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-semibold text-slate-700">Status</label><select name="status" class="mt-2 block w-full rounded-xl border-slate-300 shadow-sm focus:border-blue-500 focus:ring-blue-500"><option value="1" @selected(old('status', $permission->status) == 1)>Active</option><option value="0" @selected(old('status', $permission->status) == 0)>Inactive</option></select></div></div><div class="mt-8 flex justify-end gap-3 border-t border-slate-100 pt-6"><a href="{{ route('permissions.index') }}" class="rounded-xl px-4 py-2.5 text-sm font-semibold text-slate-600 hover:bg-slate-100">Cancel</a><button class="rounded-xl bg-blue-600 px-5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-blue-700">{{ $permission->exists ? 'Save changes' : 'Create permission' }}</button></div></form></div>
</x-app-layout>
