<x-app-layout>
    <div class="mx-auto max-w-4xl"><div class="mb-8"><a href="{{ route('roles.index') }}" class="text-sm font-semibold text-blue-600 hover:text-blue-800">← Back to roles</a><h1 class="mt-3 text-3xl font-bold text-slate-900">{{ $role->exists ? 'Edit role' : 'Create role' }}</h1><p class="mt-2 text-sm text-slate-500">Set the role name, status, and access permissions.</p></div><form method="POST" action="{{ $role->exists ? route('roles.update', $role) : route('roles.store') }}" class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">@csrf @if($role->exists) @method('PUT') @endif <div class="grid gap-6 sm:grid-cols-2"><div><label class="text-sm font-semibold text-slate-700">Role name</label><input name="display_name" value="{{ old('display_name', $role->display_name) }}" required class="mt-2 block w-full rounded-xl border-slate-300 shadow-sm focus:border-blue-500 focus:ring-blue-500">@error('display_name')<p class="mt-1 text-sm text-rose-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-semibold text-slate-700">Status</label><select name="status" class="mt-2 block w-full rounded-xl border-slate-300 shadow-sm focus:border-blue-500 focus:ring-blue-500"><option value="1" @selected(old('status', $role->status) == 1)>Active</option><option value="0" @selected(old('status', $role->status) == 0)>Inactive</option></select></div></div><div class="mt-8"><div class="mb-4"><h2 class="text-lg font-bold text-slate-900">Permissions</h2><p class="text-sm text-slate-500">Choose what users assigned to this role can do.</p></div><div class="grid gap-3 sm:grid-cols-2">@foreach($permissions->groupBy('group') as $group => $groupPermissions)<fieldset class="rounded-xl border border-slate-200 p-4"><legend class="px-1 text-sm font-bold text-slate-700">{{ $group ?: 'General' }}</legend>@foreach($groupPermissions as $permission)<label class="mt-3 flex cursor-pointer items-center gap-3"><input type="checkbox" name="permissions[]" value="{{ $permission->id }}" @checked(in_array($permission->id, old('permissions', $role->permissions->pluck('id')->all()))) class="rounded border-slate-300 text-blue-600 focus:ring-blue-500"><span><span class="block text-sm font-medium text-slate-800">{{ $permission->display_name }}</span><span class="block text-xs text-slate-500">{{ $permission->name }}</span></span></label>@endforeach</fieldset>@endforeach</div>@error('permissions')<p class="mt-2 text-sm text-rose-600">{{ $message }}</p>@enderror</div><div class="mt-8 flex justify-end gap-3 border-t border-slate-100 pt-6"><a href="{{ route('roles.index') }}" class="rounded-xl px-4 py-2.5 text-sm font-semibold text-slate-600 hover:bg-slate-100">Cancel</a><button class="rounded-xl bg-blue-600 px-5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-blue-700">{{ $role->exists ? 'Save changes' : 'Create role' }}</button></div></form></div>
</x-app-layout>
